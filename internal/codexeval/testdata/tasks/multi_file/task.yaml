id: multi_file
title: Multi-file deterministic edit
category: edit
suites:
  - codex-smoke
  - codex-core
  - codex-real-upstream
timeout: 180s
attempts: 2
env:
  LLAMA_SHIM_CODEX_MATRIX_WORKDIR: ${workspace}
prompt: |
  This is the Codex task matrix multi file case. Use exec_command to inspect the workspace, update both files exactly as follows, then run another exec_command to verify the final contents before replying.

  app/config.txt must contain:
  mode=matrix
  feature=enabled

  app/status.txt must contain:
  status=updated

  Do not rely only on file edits. You must emit at least one command execution event after editing, such as reading both files back.
  Then reply MULTIFILE.
expected:
  final_text_contains:
    - MULTIFILE
  files:
    - path: app/config.txt
      equals: |
        mode=matrix
        feature=enabled
    - path: app/status.txt
      equals_trim_space: |
        status=updated
  codex_events:
    - item.started:command_execution
    - item.completed:agent_message
    - turn.completed
  forbidden_output:
    - "<|tool_call"
    - "<|tool_calls_section"

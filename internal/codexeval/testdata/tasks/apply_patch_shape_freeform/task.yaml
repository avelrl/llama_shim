id: apply_patch_shape_freeform
title: Apply patch freeform request shape
category: shim-native
suites:
  - codex-shim-native-apply-patch-freeform
timeout: 120s
attempts: 1
prompt: |
  This is the Codex apply_patch freeform request-shape case. Complete the turn with a minimal response.
expected:
  files:
    - path: README.md
      contains: apply-patch-freeform
  codex_events:
    - item.completed:agent_message
    - turn.completed
  request_shapes:
    - name: freeform apply_patch tool advertised
      transport: http
      method: POST
      path: /v1/responses
      min_count: 1
      required_body_fields:
        - input
        - model
        - tools
      required_tool_names:
        - apply_patch
      required_tool_name_types:
        - apply_patch:custom
      forbidden_tool_name_types:
        - apply_patch:function
  forbidden_output:
    - "<|tool_call"
    - "<|tool_calls_section"
